# Wire up the mock API for local dev

Copy-paste, fill in the blank:

```
Point environment.development.ts's apiUrl at http://localhost:3000 and show me how to
run npm run mock-api alongside npm start so <feature> can fetch from src/assets/db.json.
```
