# Add a PrimeNG-based feature

Copy-paste, fill in the blanks:

```
Add a <feature, e.g. "user list page"> using PrimeNG components. This project has
PrimeNG installed but not yet wired up (no providePrimeNG call in app.config.ts) —
check the current PrimeNG v22 standalone-components setup docs first, then add the
provider and the component. Use <specific PrimeNG components if known, e.g. p-table,
p-button>.
```
