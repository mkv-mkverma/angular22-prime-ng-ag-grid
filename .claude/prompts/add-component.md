# Add a new standalone component

Copy-paste, fill in the blanks:

```
Create a standalone Angular component named <ComponentName> in src/app/<path>.
Follow this repo's conventions: standalone (no NgModule), app-prefixed kebab-case
selector, Prettier 100-char/single-quote style. Wire it into app.routes.ts at path
"<route-path>".
```
