# Add an AG Grid table

Copy-paste, fill in the blanks:

```
Add a data grid for <data/entity> using ag-grid-angular (Enterprise). This project has
AG Grid Enterprise installed but not yet wired up — check the current ag-grid-angular
v36 standalone setup docs for module registration/licensing before wiring it in. Column
definitions: <list fields>. Data source: <static / apiUrl from environment>.
```
